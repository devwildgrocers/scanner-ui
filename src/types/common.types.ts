export interface TeamMember {
  id: string;
  name: string;
}

export interface Cart {
  id: string;
  cartId: string;
  status: string;
}

export interface Product {
  id: string;
  name: string;
  barcode: string;
}
